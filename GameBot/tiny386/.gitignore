# Emulator disk images and installer media.
#
# These are build/runtime artifacts, not source: win95_extra_files.iso is
# ~1.2 GB and each win95_*.img is ~100 MB. GitHub rejects any single file over
# 100 MB outright, and a large blob that does get in stays in history forever
# even if it is deleted later -- so they are kept out deliberately rather than
# by oversight. Fetch or rebuild them locally.
*.img
*.iso
*.img.zst
*.zst

# ...except the two drives win95all.ini actually boots. These are 90 MB and
# 95 MB -- over GitHub's 50 MB warning threshold but under its 100 MB hard
# limit -- and are committed deliberately so a clone can boot without hunting
# for them. Note hdc (win95_d_drive_cc.img, 99 MB) is also uncommented in
# win95all.ini and is NOT here; add it the same way if it turns out to be
# required rather than optional.
!win95_c_drive.img
!win95_d_drive_all.img

# Built wasm + firmware blobs -- produced from ../../../tiny386 by its Makefile.
*.wasm
*.bin

# Timestamped safety copies made while editing.
.backup-*/
